[deploy]
startCommand = "python3 -m uvicorn consentcrawl.server:app --host 0.0.0.0 --port $PORT --timeout-keep-alive 120"
healthcheckPath = "/health"
healthcheckTimeout = 100
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10
